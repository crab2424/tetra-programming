発展
・モード切り替え時，タイマーが残っているためリセット
・音楽，SE追加

対戦CPU
1対1
レベルを5段階で変える

lv1
初心者向け
ソフドロのみ，ホールドなし，NEXT視認0個

lv2
初級者向け
ハードロ，ホールド使用，NEXT視認1個，tspinなし

lv3
中級者向け
テンプレ使用（制限），t-spinあり，NEXT視認3個，置きミス率5%

lv4
上級者向け
TDテンプレ使用，置きミス率3%，NEXT視認5個

lv5
最上級者向け
相殺外し，置きミス率1%

学習

段差比較

1段差　+2
2段差  -2
3段差  -5
4段差以降毎  -10

ーーーーーーーーーーーーーーーーーーーーーーーーーーーメモ

・一手目で穴を塞ぐと負

・後から回転入れの検索について，空間によってできた特定のミノが1〜2手以内にあるか確かめ，
    ある場合はそのミノを回転入れさせた盤面を評価することにする．

・BtB解除のパラメータを負，継続で正

・SRS含めた全探索を行う

・パフェ判定（6~8列以下）
ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

emcc cpu2.cpp -O3 -s WASM=1 -s EXPORTED_RUNTIME_METHODS='["ccall", "cwrap"]' -o cpu_wasm.js
emcc cpu2.cpp -O3 -s WASM=1 -o cpu_wasm.js

* `-O3` は「最強の最適化（最高速）をかけてくれ」という命令です。
* `-o cpu_wasm.js` とすることで、Web Worker（`cpu_worker.js`）が読み込むための架け橋ファイルと、実際のバイナリファイル（`cpu_wasm.wasm`）の2つが自動生成されます。

 TETLABOのcpuをweb worker+wasmで作成している途中です．jsに重み（パラメータ）を設定し，cppに渡して計算しています．動作はできているのでファイル周りのことは大丈夫です．この2つのファイルを参照し，指示に従って編集してください．
