発展
・音楽，SE追加

異種

バグ

ーーーーーーーーーーーーーーーーーーーーーーーーーーーメモ

・TST暗記   5

・パフェ判定（6~8列以下）   ?
・上部の回し   ?

・堀連 5

・lv4で対テトにカウンターを組む



ぷよ（一旦直接攻撃のみ）

4,5連結の単色1連鎖ではどれだけ打っても火力が発生しない
6連結の単色1連鎖は2回打つと火力が発生する

相殺時のことを考えると落下ボーナス（もしくはスコア持ち越し）が発生してそう


テト

4, 5, 6, 8, 10, 13, 16, 20, 24, 28, 33, 38, 43, 49, 
ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

emcc cpu2.cpp -O3 -s WASM=1 -s EXPORTED_RUNTIME_METHODS='["ccall", "cwrap"]' -o cpu_wasm.js
emcc cpu2.cpp -O3 -s WASM=1 -o cpu_wasm.js

emcc cpu5.cpp -O2 -o cpu_wasm5.js \
  -s EXPORTED_FUNCTIONS='["_searchBestMoveWasm","_my_malloc","_my_free"]' \
  -s EXPORTED_RUNTIME_METHODS='["ccall","cwrap"]' \
  -s ALLOW_MEMORY_GROWTH=1 \
  -s INITIAL_MEMORY=16777216

emcc cpu1.cpp -O2 -s WASM=1 \
  -s EXPORTED_FUNCTIONS='["_searchBestMovePuyoWasm","_my_malloc","_my_free"]' \
  -s EXPORTED_RUNTIME_METHODS='["HEAPU8","HEAP32"]' \
  -s INITIAL_MEMORY=8388608 \
  -o cpu_wasm1.js


これで、C++側のメモリ動的確保がゼロになり、完全に安定して動作するようになります。

* `-O3` は「最強の最適化（最高速）をかけてくれ」という命令です。
* `-o cpu_wasm.js` とすることで、Web Worker（`cpu_worker.js`）が読み込むための架け橋ファイルと、実際のバイナリファイル（`cpu_wasm.wasm`）の2つが自動生成されます。

 TETLABOのぷよcpuをweb worker+wasmで作成している途中です．jsに重み（パラメータ）を設定し，cppに渡して計算しています．動作はできているのでファイル周りのことは大丈夫です．この3つのファイルを参照し，指示に従って編集してください．ファイルに示されている処理は極力変更・削除しないようにしてください．